window.LyoBench = window.LyoBench || { reports: {}, history: {}, historyIndex: {} };
window.LyoBench.registry = [{"name": "query-api", "title": "Query API (k6)", "type": "load", "description": "k6 load/stress/spike/soak against the Lyo person API. /person/QueryConcrete returns full Person entities; /person/QueryProject returns field-projected rows; POST /Query is root From/Joins sparse projection. Every iteration pages 100-300 persons (varied by VU/iteration to bypass caches). Randomized multi-key sorting over unindexed columns is disabled by default (only filter_sort carries a fixed sort); other cases use the server default (PK) order. The 'cases' below describe the request shape behind each scenario; hotspots reference these case ids."}, {"name": "csv", "title": "CSV", "type": "micro", "description": "CSV write, read, split, and merge paths for Lyo.Csv over a flat 7-column SampleRecord (int/string/decimal/bool/DateTime). Covers typed object lists (bytes and string), streaming parse, DataTable round-trips, row-based split/combine (bytes and files), and multi-part merge. RowCount is the number of records; the data structure (columns and types) is captured in each class's data shape. The SampleRecord here is flat - nested objects/collections would add per-row flattening cost not represented by row count alone."}, {"name": "cache", "title": "Caching", "type": "micro", "description": "Cache hot-path costs for Lyo.Cache: local IMemoryCache vs FusionCache for object get/set, the serialized byte-payload path (JSON +/- compression framing) across payload sizes, and FusionCache with a Redis backplane (Docker). Object cases use a small CachePayload; payload cases vary DataSize (the length of a compressible string body) to show framing/serialization cost."}, {"name": "compression", "title": "Compression", "type": "micro", "description": "Compress/decompress throughput for Lyo.Compression across GZip, Deflate, Zstd, Snappier, LZ4, LZMA, BZip2, XZ (and Brotli/ZLib on net10.0). Important caveat: the input is cryptographically random bytes of the given DataSize, which is effectively incompressible - these benchmarks measure raw algorithm speed and framing overhead, NOT achievable compression ratio on real data. Decompress cases reuse output produced once in setup."}, {"name": "encryption", "title": "Encryption", "type": "micro", "description": "Symmetric authenticated-encryption throughput for Lyo.Encryption: AES-GCM/CCM/SIV and ChaCha20-Poly1305/XChaCha20-Poly1305, encrypt and decrypt. Each run encrypts/decrypts a buffer of cryptographically random bytes of the given DataSize (1 KB to 100 MB) using a fixed local key store, so timings reflect raw per-byte cipher throughput."}, {"name": "hashing", "title": "Hashing", "type": "micro", "description": "SHA-2/MD5 content digests, non-cryptographic checksums (CRC-32/CRC-32C/CRC-64/Adler-32), HMAC keyed hashes, incremental stream hashing, the static-vs-injectable hashing surface, and hex encode/decode for Lyo.Hashing. Every payload is a buffer of cryptographically random bytes of the given DataSize, so timings reflect raw per-byte throughput (random data is incompressible and cannot be short-circuited)."}, {"name": "lock", "title": "Locking", "type": "micro", "description": "Distributed-lock primitives for Lyo.Lock: uncontended acquire/release and execute-with-lock for the in-process LocalLockService vs the distributed RedisLockService (throwaway Docker Redis), plus a bounded contended local scenario. Each operation uses a unique key (GUID) so cases are uncontended unless stated; the contended case measures coordination overhead under N contenders, not raw throughput."}, {"name": "query", "title": "Query & CRUD", "type": "micro", "description": "Lyo.Query engine internals plus end-to-end CRUD. In-memory suites run the where-clause engine (expression build, filtering, single-entity match), ordering, object-to-DTO mapping, and the projection pipeline over generated BenchPerson rows (RowCount). The CRUD suite runs Query/Get/Patch/Create/Delete against a real PostgreSQL database (Testcontainers, Docker) paging Amount rows. Each class's data shape captures the entity/model structure being exercised, including nested collections."}, {"name": "xlsx", "title": "XLSX", "type": "micro", "description": "XLSX write (streaming OpenXML), read (ExcelDataReader), convert, split, and merge paths for Lyo.Xlsx over a flat 7-column SampleRecord. Split benchmarks cover row chunks and per-worksheet outputs; merge benchmarks cover PreserveSheets and ConcatenateRows. Row counts are smaller than CSV because XLSX is markedly heavier per row. RowCount is the number of records; the column set and types are captured in each class's data shape."}];
