{
  "id": "Lyo.Health",
  "name": "Lyo.Health",
  "area": "Core",
  "tagline": "Interface for services that can report their health. Services implement `IHealth` and expose health directly—no central health service.",
  "description": "Interface for services that can report their health. Services implement `IHealth` and expose health directly—no central health service. Health returns `HealthResult` with status,\ntimings, and optional metadata.",
  "features": [],
  "examples": [
    {
      "title": "Usage",
      "language": "csharp",
      "code": "// File storage\nvar fileStorage = app.Services.GetRequiredService<IFileStorageService>();\nvar result = await fileStorage.CheckHealthAsync();\n// result.IsHealthy, result.Duration, result.Metadata, result.Message\n\n// Cache\nvar cache = app.Services.GetRequiredService<ICacheService>();\nvar result = await cache.CheckHealthAsync();\n\n// RabbitMQ\nvar mq = app.Services.GetRequiredService<IMqService>();\nvar result = await mq.CheckHealthAsync();"
    },
    {
      "title": "Contract",
      "language": "csharp",
      "code": "public interface IHealth\n{\n    string HealthCheckName { get; }\n    Task<HealthResult> CheckHealthAsync(CancellationToken ct = default);\n}"
    },
    {
      "title": "`HealthResult`",
      "language": "csharp",
      "code": "HealthResult.Healthy(sw.Elapsed, message: null, metadata: new Dictionary<string, object?> { [\"database\"] = \"audit\" });\nHealthResult.Unhealthy(sw.Elapsed, \"Database connection failed\", metadata: null, exception: ex);"
    }
  ],
  "sections": [
    {
      "type": "markdown",
      "title": "Contract",
      "body": "- **`HealthCheckName`** is a short identifier (e.g. `\"filestorage\"`, `\"cache\"`, `\"rabbitmq\"`, `\"audit-postgres\"`, `\"change-tracker-postgres\"`) used to label probe output in hosts.\n- **`CheckHealthAsync`** is the live probe; it should be cheap, short-circuiting, and honor the supplied `CancellationToken`."
    },
    {
      "type": "table",
      "title": "`HealthResult`",
      "headers": [
        "Member",
        "Type",
        "Notes"
      ],
      "rows": [
        [
          "`IsHealthy`",
          "`bool`",
          "`true` for healthy, `false` for unhealthy."
        ],
        [
          "`Duration`",
          "`TimeSpan`",
          "How long the probe took (typically measured with `Stopwatch`)."
        ],
        [
          "`CheckedAt`",
          "`DateTime`",
          "UTC instant the probe completed (set by the factory helpers)."
        ],
        [
          "`Message`",
          "`string?`",
          "Optional human-readable summary; populated with the exception message on failure."
        ],
        [
          "`Metadata`",
          "`IReadOnlyDictionary<string, object?>?`",
          "Connection info, schema, version, key-id, etc."
        ],
        [
          "`Exception`",
          "`Exception?`",
          "Captured exception when the probe threw."
        ]
      ],
      "lead": "`HealthResult` is an immutable, sealed class that captures the outcome of a probe:",
      "trail": "Use the static factories rather than constructing directly:"
    },
    {
      "type": "paragraph",
      "title": "Usage",
      "text": "Get health from the service directly: Service interfaces (`IFileStorageService`, `ICacheService`, `IMqService`) extend `IHealth`—health comes from the service, no separate registration. Hosts that need an aggregate view typically resolve `IEnumerable<IHealth>` and fan out `CheckHealthAsync` in parallel, then publish the resulting `HealthResult` collection."
    }
  ],
  "links": [],
  "readmePath": "Lyo.Net/Core/Health/Lyo.Health/README.md",
  "dependencies": [
    {
      "name": "Lyo.Exceptions",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    }
  ],
  "targetFrameworks": [
    "netstandard2.0",
    "net10.0"
  ]
}
