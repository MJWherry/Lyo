{
  "name": "lyo-query",
  "version": "0.1.0",
  "description": "Shared Lyo Query models and where-clause helpers for TypeScript clients and UI.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.2.0"
  }
}
