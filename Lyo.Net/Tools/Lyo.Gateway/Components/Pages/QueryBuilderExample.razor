@using Lyo.Query.Web.Components
@using Constants = Constants
@attribute [Route("/" + Constants.Page.QueryBuilder)]
@rendermode InteractiveServer
@inherits AuthorizedPage

<PageTitle>Query Builder Example</PageTitle>

<QueryBuilderWorkbench Routes="@Routes"/>

@code {

    protected override string PageName { get; set; } = "Query Builder Example";

    // Entity routes for /QueryConcrete|/QueryProject; empty "" seeds root /Query (dynamic base) for PeopleDbContext.
    public readonly Dictionary<string, List<string>> Routes = new() {
        ["http://localhost:5251"] = ["Person", ""],
        ["http://localhost:5074"] = ["Docket", "Docket/Charge"]
    };

}
