@using Lyo.Web.Components.DataGrid
@using Lyo.Web.Components.Export
@using Lyo.Web.Components.Export.Csv
@using Lyo.Web.Components.Export.Xlsx
@using Lyo.Web.Components.Models
@using Constants = Lyo.TestGateway.Models.Constants
@inject IApiClient ApiClient

<LyoDataGridProjected ApiClient="ApiClient"
                      GridKey="EndatoPsPersonGrid"
                      Route="@Constants.EndatoPs.Person"
                      KeySelector="@GetKey"
                      FilterPropertyDefinitions="@_propertyDefinitions">
    <BulkExportControls>
        <LyoDataGridExportMenu Host="context">
            <ExportCsvMenuItem Host="context"/>
            <ExportJsonMenuItem Host="context"/>
            <ExportXlsxMenuItem Host="context"/>
        </LyoDataGridExportMenu>
    </BulkExportControls>
    <Columns>
        <LyoProjectedColumn Field="Id" Title="ID" HeaderStyle="width: 140px;" CellStyle="white-space: nowrap;" Identifier="true"/>
        <LyoProjectedColumn Field="FirstName" Title="First Name" QuickSearchPropertyName="FirstName"/>
        <LyoProjectedColumn Field="MiddleName" Title="Middle Name" HiddenByDefault="true"/>
        <LyoProjectedColumn Field="LastName" Title="Last Name" QuickSearchPropertyName="LastName"/>
        <LyoProjectedColumn Field="DateOfBirth" Title="DoB"/>
        <LyoProjectedColumn Field="Addresses.Count" Title="Addresses"/>
        <LyoProjectedColumn Field="EmailAddresses.Count" Title="Emails"/>
        <LyoProjectedColumn Field="PhoneNumbers.Count" Title="Phones"/>
        <LyoProjectedColumn Field="QueryId" Title="Query ID" HiddenByDefault="true" HeaderStyle="width: 140px;" CellStyle="white-space: nowrap;" Identifier="true"/>
    </Columns>
</LyoDataGridProjected>

@code {
    private readonly List<FilterPropertyDefinition> _propertyDefinitions = [new("FirstName"), new("LastName")];

    private static object[] GetKey(object? item)
    {
        if (item == null)
            return [];

        var id = ProjectedValueHelper.GetValue(item, "Id");
        return id != null ? [id] : [];
    }

}
