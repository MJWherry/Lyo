{
  "id": "Lyo.Lock",
  "name": "Lyo.Lock",
  "area": "Core",
  "tagline": "Key-based **exclusive locks** and **keyed semaphores** (bounded concurrency per key) with a small abstraction layer and in-memory implementations for a single process.",
  "description": "Key-based **exclusive locks** and **keyed semaphores** (bounded concurrency per key) with a small abstraction layer and in-memory implementations for a single process.",
  "features": [
    "**`ILockService`** — acquire/release by string key, or `ExecuteWithLockAsync` helpers that throw `TimeoutException` if the lock is not obtained.",
    "**`LocalLockService`** — one holder per normalized key using `SemaphoreSlim`.",
    "**`IKeyedSemaphoreService`** — up to `maxConcurrency` simultaneous permit holders **per key**.",
    "**`LocalKeyedSemaphoreService`** — per-key `SemaphoreSlim` with ref-counted cleanup when idle.",
    "**Key normalization** — by default keys are compared case-insensitively (`ToLowerInvariant`); optional skip when keys are already normalized.",
    "**DI** — `AddLocalLock`, `AddLocalLockFromConfiguration`, `AddLocalKeyedSemaphore`, `AddLocalKeyedSemaphoreFromConfiguration`.",
    "**Metrics** — optional timers/counters via `Lyo.Metrics` when `EnableMetrics` is true and `IMetrics` is registered."
  ],
  "examples": [
    {
      "title": "Quick start",
      "language": "csharp",
      "code": "using Lyo.Lock;\nusing Microsoft.Extensions.DependencyInjection;\n\nservices.AddLocalLock();\nservices.AddLocalKeyedSemaphore();\n\n// Optional: bind appsettings → LockOptions / KeyedSemaphoreOptions\nservices.AddLocalLockFromConfiguration(configuration);\nservices.AddLocalKeyedSemaphoreFromConfiguration(configuration);"
    },
    {
      "title": "Quick start (2)",
      "language": "csharp",
      "code": "// Exclusive lock: null if timeout\nvar handle = await lockService.AcquireAsync(\"order:123\", timeout: TimeSpan.FromSeconds(5));\nif (handle is not null)\n{\n    await using (handle)\n        await ProcessOrderAsync();\n}\n\n// Throws TimeoutException if not acquired\nawait lockService.ExecuteWithLockAsync(\"order:123\", async ct => await ProcessOrderAsync(ct));\n\n// Up to 3 concurrent operations for the same key (same process only)\nawait semaphoreService.ExecuteAsync(\"export:tenant-1\", 3, async ct => await RunExportAsync(ct));"
    },
    {
      "title": "`KeyedSemaphoreOptions` (`KeyedSemaphoreOptions` section)",
      "language": "json",
      "code": "{\n  \"LockOptions\": {\n    \"DefaultAcquireTimeout\": \"00:00:30\",\n    \"DefaultLockDuration\": \"00:01:00\",\n    \"KeyPrefix\": \"lyo:lock:\",\n    \"EnableMetrics\": false,\n    \"SkipKeyNormalization\": false\n  },\n  \"KeyedSemaphoreOptions\": {\n    \"DefaultAcquireTimeout\": \"00:00:30\",\n    \"SkipKeyNormalization\": false,\n    \"EnableMetrics\": false\n  }\n}"
    }
  ],
  "sections": [
    {
      "type": "table",
      "title": "When to use what",
      "headers": [
        "Primitive",
        "Type",
        "Scope",
        "Typical use"
      ],
      "rows": [
        [
          "`ILockService` / `LocalLockService`",
          "Mutex per key",
          "One process",
          "Guard mutations to one aggregate, avoid duplicate work, serialize handlers per entity ID"
        ],
        [
          "`IKeyedSemaphoreService` / `LocalKeyedSemaphoreService`",
          "Counting semaphore per key",
          "One process",
          "Cap concurrent exports/API calls/backfills *per tenant or resource key* without global rate limits"
        ]
      ],
      "trail": "For **multiple servers or processes**, register a distributed `ILockService` (see [`Lyo.Lock.Redis`](../Lyo.Lock.Redis/README.md)). Keyed semaphores in this package remain **local\nonly**."
    },
    {
      "type": "paragraph",
      "title": "Quick start",
      "text": "Inject `ILockService` and/or `IKeyedSemaphoreService`:"
    },
    {
      "type": "list",
      "title": "Rules for keyed semaphores",
      "items": [
        "Use a **stable `maxConcurrency`** for a given key while any permit is held or waiters exist. If you pass a different `maxConcurrency` for an active key, `LocalKeyedSemaphoreService` throws (`InvalidOperationException`) instead of undefined behavior.",
        "Cancellation tokens on `AcquireAsync` / `ExecuteAsync` are honored while waiting."
      ]
    },
    {
      "type": "table",
      "title": "`LockOptions` (`LockOptions` section)",
      "headers": [
        "Property",
        "Default",
        "Description"
      ],
      "rows": [
        [
          "`DefaultAcquireTimeout`",
          "30s",
          "Max wait for `AcquireAsync` / `ExecuteWithLockAsync`."
        ],
        [
          "`DefaultLockDuration`",
          "60s",
          "Used by **distributed** locks (Redis TTL). Ignored by `LocalLockService`."
        ],
        [
          "`KeyPrefix`",
          "`lyo:lock:`",
          "Prefix for Redis keys; harmless for local-only usage."
        ],
        [
          "`SkipKeyNormalization`",
          "`false`",
          "When `true`, keys are not lowercased (caller must ensure consistent casing)."
        ],
        [
          "`EnableMetrics`",
          "`false`",
          "Record lock timings/counters when `IMetrics` is available."
        ]
      ]
    },
    {
      "type": "table",
      "title": "`KeyedSemaphoreOptions` (`KeyedSemaphoreOptions` section)",
      "headers": [
        "Property",
        "Default",
        "Description"
      ],
      "rows": [
        [
          "`DefaultAcquireTimeout`",
          "30s",
          "Max wait for a permit."
        ],
        [
          "`SkipKeyNormalization`",
          "`false`",
          "Same semantics as lock options."
        ],
        [
          "`EnableMetrics`",
          "`false`",
          "Record semaphore timings/counters when `IMetrics` is available."
        ]
      ],
      "trail": "Example `appsettings.json`:"
    },
    {
      "type": "table",
      "title": "Metrics (`Constants`)",
      "headers": [
        "Name",
        "Role"
      ],
      "rows": [
        [
          "`lock.acquire.duration`",
          "Wait time for acquisition"
        ],
        [
          "`lock.acquire.success` / `lock.acquire.failure`",
          "Counter"
        ],
        [
          "`lock.release.duration`",
          "Release timing"
        ],
        [
          "`lock.execute.duration`",
          "Wall time for `ExecuteWithLockAsync`"
        ]
      ],
      "lead": "When metrics are enabled and `IMetrics` is registered, names match `Lyo.Lock.Constants`:\n\n**Locks (`Constants.Metrics`)**",
      "trail": "**Semaphores (`Constants.SemaphoreMetrics`)**\n\n| Name | Role |\n|-----------------------------------------------------------|------------------------------|\n| `semaphore.acquire.duration` | Wait time for a permit |\n| `semaphore.acquire.success` / `semaphore.acquire.failure` | Counter |\n| `semaphore.release.duration` | Release timing |\n| `semaphore.execute.duration` | Wall time for `ExecuteAsync` |\n\nTag dimension: `key` — logical key string as passed by the caller (see XML docs on `Constants`)."
    },
    {
      "type": "list",
      "title": "API summary — `ILockService`",
      "items": [
        "`AcquireAsync` — returns `ILockHandle?` (`null` on timeout).",
        "`ExecuteWithLockAsync` / `ExecuteWithLockAsync<T>` — acquire, run delegate, release; throw `TimeoutException` if not acquired."
      ]
    },
    {
      "type": "list",
      "title": "API summary — `ILockHandle` / `IPermitHandle`",
      "items": [
        "`ReleaseAsync` — idempotent after first release.",
        "`Dispose` / `DisposeAsync` — release (sync dispose may block briefly on internal `ReleaseAsync`)."
      ]
    },
    {
      "type": "list",
      "title": "API summary — `IKeyedSemaphoreService`",
      "items": [
        "`AcquireAsync` — returns `IPermitHandle?` on timeout.",
        "`ExecuteAsync` / `ExecuteAsync<T>` — throw `TimeoutException` if no permit."
      ]
    }
  ],
  "links": [],
  "readmePath": "Lyo.Net/Core/Lock/Lyo.Lock/README.md",
  "benchmarks": {
    "suite": "lock"
  },
  "dependencies": [
    {
      "name": "Lyo.Exceptions",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Metrics",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Microsoft.Extensions.Configuration.Binder",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "direct",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.DependencyInjection.Abstractions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "direct",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.Logging.Abstractions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "direct",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.Options.ConfigurationExtensions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    }
  ],
  "targetFrameworks": [
    "netstandard2.0",
    "net10.0"
  ]
}
