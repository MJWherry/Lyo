{
  "id": "Lyo.Testing.Containers",
  "name": "Lyo.Testing.Containers",
  "area": "Core",
  "tagline": "xUnit v3 fixture helpers around **Testcontainers** so integration tests can spin up real backing services without hand-rolling lifecycle plumbing.",
  "description": "xUnit v3 fixture helpers around **Testcontainers** so integration tests can spin up real backing services without hand-rolling lifecycle plumbing. The shipped helpers cover **PostgreSQL** and **RabbitMQ** — other backends can be added by following the same shape.\n\n> **Internal-only:** `IsPackable` is `false` and `xunit.v3.extensibility.core` is a project-level dependency. Reference this project from test projects; do not pack it.",
  "features": [],
  "examples": [
    {
      "title": "Quick start — xUnit v3 class fixture",
      "language": "csharp",
      "code": "using Lyo.Testing.Containers;\n\npublic sealed class MyFeatureFixture : PostgresContainerFixtureBase\n{\n    protected override async ValueTask OnContainerStartedAsync(string connectionString, CancellationToken ct)\n    {\n        // e.g. apply EF Core migrations against the new database\n        using var ctx = new MyDbContext(BuildContextOptions(connectionString));\n        await ctx.Database.MigrateAsync(ct);\n    }\n\n    private static DbContextOptions<MyDbContext> BuildContextOptions(string connectionString) =>\n        new DbContextOptionsBuilder<MyDbContext>().UseNpgsql(connectionString).Options;\n}\n\npublic sealed class MyFeatureTests(MyFeatureFixture fixture) : IClassFixture<MyFeatureFixture>\n{\n    [Fact]\n    public async Task ConnectsAndQueries()\n    {\n        using var ctx = new MyDbContext(new DbContextOptionsBuilder<MyDbContext>()\n            .UseNpgsql(fixture.ConnectionString).Options);\n        Assert.NotNull(await ctx.Database.GetDbConnection().ServerVersionAsync());\n    }\n}"
    },
    {
      "title": "Customizing the container",
      "language": "csharp",
      "code": "public sealed class TrgmFixture() : PostgresContainerFixtureBase(new PostgresContainerOptions {\n    Image = \"postgres:17-alpine\",\n    ConfigureBuilder = builder => builder\n        .WithEnvironment(\"POSTGRES_DB\", \"lyo_tests\")\n        .WithCommand(\"postgres\", \"-c\", \"shared_preload_libraries=pg_trgm\"),\n});"
    },
    {
      "title": "Standalone use (no fixture)",
      "language": "csharp",
      "code": "await using var container = new PostgresTestContainer();\nawait container.StartAsync(ct);\nvar connectionString = container.ConnectionString;\n\nawait using var broker = new RabbitMqTestContainer();\nawait broker.StartAsync(ct);\nvar (host, port, adminUrl) = (broker.Host, broker.Port, broker.AdminUrl);"
    }
  ],
  "sections": [
    {
      "type": "table",
      "title": "Public surface",
      "headers": [
        "Type",
        "Role"
      ],
      "rows": [
        [
          "**`PostgresTestContainer`**",
          "`IAsyncDisposable` wrapper around `PostgreSqlContainer`. Call `StartAsync(CancellationToken)` once, then read `ConnectionString`. Throws `InvalidOperationException` when `ConnectionString` is read before `StartAsync`."
        ],
        [
          "**`PostgresContainerOptions`**",
          "`Image` (defaults to `postgres:16-alpine`) and optional `ConfigureBuilder(Action<PostgreSqlBuilder>)` hook for custom env vars, networks, volumes, etc."
        ],
        [
          "**`PostgresContainerFixtureBase`**",
          "Abstract xUnit `IAsyncLifetime` fixture: starts a shared container, invokes `OnContainerStartedAsync(connectionString, ct)`, exposes `ConnectionString`, and calls `OnContainerDisposingAsync(ct)` before tearing the container down. Cancellation is sourced from `TestContext.Current.CancellationToken`."
        ],
        [
          "**`RabbitMqTestContainer`**",
          "`IAsyncDisposable` wrapper around `RabbitMqContainer`. After `StartAsync`, exposes `Host`, `Port` (mapped AMQP), `AdminUrl` (mapped management HTTP API), `Username`/`Password`, and the AMQP `ConnectionString`."
        ],
        [
          "**`RabbitMqContainerOptions`**",
          "`Image` (defaults to `rabbitmq:4-management-alpine` — must be a management-enabled image for `AdminUrl` to work) and optional `ConfigureBuilder(Action<RabbitMqBuilder>)` hook."
        ],
        [
          "**`RabbitMqContainerFixtureBase`**",
          "Abstract xUnit `IAsyncLifetime` fixture mirroring the Postgres one: starts a shared broker, invokes `OnContainerStartedAsync(container, ct)`, exposes the endpoint properties, and calls `OnContainerDisposingAsync(ct)` before teardown."
        ]
      ]
    },
    {
      "type": "markdown",
      "title": "Sharing one container across an assembly",
      "body": "```csharp\n[assembly: AssemblyFixture(typeof(MyFeatureFixture))]\n```\n\n`PostgresContainerFixtureBase` is safe to reuse as an assembly fixture because `OnContainerStartedAsync` runs exactly once and `OnContainerDisposingAsync` runs once at teardown."
    },
    {
      "type": "markdown",
      "title": "RabbitMQ fixture",
      "body": "`RabbitMqContainerFixtureBase` follows the same pattern. `Lyo.MessageQueue.RabbitMq.Tests` uses it to run\n`RabbitMqService` against a real broker:\n\n```csharp\npublic sealed class RabbitMqBrokerFixture : RabbitMqContainerFixtureBase\n{\n    public RabbitMqService CreateService(Action<RabbitMqOptions>? configure = null)\n    {\n        var options = new RabbitMqOptions { Host = Host, Port = Port, AdminUrl = AdminUrl, Username = Username, Password = Password };\n        configure?.Invoke(options);\n        var factory = new ConnectionFactory { HostName = options.Host, Port = options.Port, UserName = options.Username, Password = options.Password };\n        return new(options, factory);\n    }\n}\n\n[assembly: AssemblyFixture(typeof(RabbitMqBrokerFixture))]\n```\n\nThe management HTTP API (`AdminUrl`) is exposed so peek and queue-statistics operations work in tests."
    },
    {
      "type": "list",
      "title": "See also",
      "items": [
        "**`Lyo.Testing`** — shared assertion / fake / time-control helpers used by tests across the solution.",
        "**Testcontainers for .NET** documentation — for advanced builder configuration (volumes, networks, wait strategies)."
      ]
    }
  ],
  "links": [],
  "readmePath": "Lyo.Net/Core/Lyo.Testing.Containers/README.md",
  "dependencies": [
    {
      "name": "Testcontainers.PostgreSql",
      "kind": "package",
      "version": "4.13.0",
      "tags": [
        "direct",
        "third-party"
      ]
    },
    {
      "name": "Testcontainers.RabbitMq",
      "kind": "package",
      "version": "4.13.0",
      "tags": [
        "direct",
        "third-party"
      ]
    },
    {
      "name": "xunit.v3.extensibility.core",
      "kind": "package",
      "version": "3.2.2",
      "tags": [
        "direct",
        "third-party"
      ]
    }
  ],
  "targetFrameworks": [
    "net10.0"
  ]
}
