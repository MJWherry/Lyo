{
  "id": "Lyo.Scheduler",
  "name": "Lyo.Scheduler",
  "area": "Core",
  "tagline": "In-process scheduler service for executing actions at scheduled times. Supports **SetTimes**, **Interval**, **OneShot**, and **Cron** schedules (5- or 6-field expressions) with logging, metrics, and…",
  "description": "In-process scheduler service for executing actions at scheduled times. Supports **SetTimes**, **Interval**, **OneShot**, and **Cron** schedules (5- or 6-field expressions) with logging, metrics, and optional state persistence via `ISchedulerStateStore`.",
  "features": [
    "**Schedule types.** `SetTimes` (specific daily times), `Interval` (periodic within a window), `OneShot` (single run), `Cron` (5- or 6-field expressions via [`Lyo.Schedule.Models.CronExpression`](../../Schedule/Lyo.Schedule.Models/README.md)).",
    "**State persistence.** In-memory by default. Pluggable `ISchedulerStateStore` (e.g. cache-backed) for cross-restart persistence.",
    "**Logging and metrics.** `IMetrics` and `ILogger`.",
    "**Background execution.** Actions run in background by default. Optional action timeout."
  ],
  "examples": [
    {
      "title": "Usage",
      "language": "csharp",
      "code": "using Lyo.Scheduler;\nusing Lyo.Schedule.Models;\n\n// Add to DI (in-memory state store)\nservices.AddScheduler();\n\n// Or with custom options\nservices.AddScheduler(options =>\n{\n    options.CheckIntervalMs = 5_000;\n    options.ActionTimeout = TimeSpan.FromMinutes(10);\n    options.RunInBackground = true;\n});\n\n// Or with a persistent state store (e.g. cache-backed)\nservices.AddScheduler(myStateStore);\n\n// Add schedules and start\nvar scheduler = app.Services.GetRequiredService<ISchedulerService>();\n\nscheduler.AddSchedule(\n    \"daily-report\",\n    \"Daily Report\",\n    new ScheduleDefinition\n    {\n        Type = ScheduleType.SetTimes,\n        Times = [\"09:00\", \"17:00\"],\n        Timezone = \"America/New_York\"\n    },\n    async ct => await SendDailyReportAsync(ct));\n\nawait scheduler.StartAsync();"
    }
  ],
  "sections": [
    {
      "type": "table",
      "title": "`ISchedulerService` methods",
      "headers": [
        "Member",
        "Purpose"
      ],
      "rows": [
        [
          "`AddSchedule(string id, string name, ScheduleDefinition def, Func<CancellationToken,Task> action)`",
          "Register a scheduled action. Returns `true` when newly registered; `false` when an entry already exists for `id`."
        ],
        [
          "`RemoveSchedule(string id)`",
          "Remove a schedule."
        ],
        [
          "`TryGetSchedule(string id, out ScheduledAction action)` / `GetSchedules()`",
          "Inspection."
        ],
        [
          "`StartAsync(CancellationToken)` / `StopAsync(CancellationToken)`",
          "Lifecycle."
        ],
        [
          "`TriggerNowAsync(string id, CancellationToken)`",
          "Force-run an existing schedule out of cadence."
        ],
        [
          "`GetNextRun(string id, DateTimeOffset? from = null)`",
          "Next occurrence using `ScheduleCalculator` for all four schedule types."
        ]
      ],
      "trail": "`ScheduleCalculator` is the underlying evaluator: it dispatches to `GetNextRunSetTimes` / `GetNextRunInterval` / `GetNextRunOneShot` / `GetNextRunCron`. Cron evaluation first tries\n`CronFormat.IncludeSeconds` (6 fields) and falls back to the standard 5-field format."
    },
    {
      "type": "list",
      "title": "Schedule types (Lyo.Schedule.Models)",
      "items": [
        "**SetTimes.** Run at specific times each day (e.g. 09:00, 17:00).",
        "**Interval.** Run every N minutes/hours within a daily window.",
        "**OneShot.** Run once at a specific time.",
        "**Cron.** Standard 5-field or 6-field cron (`\"0 8 * * MON-FRI\"`, `\"*/30 * * * * *\"`)."
      ]
    },
    {
      "type": "table",
      "title": "Configuration",
      "headers": [
        "Option",
        "Default",
        "Description"
      ],
      "rows": [
        [
          "CheckIntervalMs",
          "10000",
          "Interval (ms) between checks for due schedules"
        ],
        [
          "EnableMetrics",
          "true",
          "Enable metrics (when IMetrics registered)"
        ],
        [
          "RunInBackground",
          "true",
          "Run actions fire-and-forget vs await"
        ],
        [
          "ActionTimeout",
          "120 min",
          "Max duration for each action; null = no timeout"
        ]
      ]
    }
  ],
  "links": [],
  "readmePath": "Lyo.Net/Core/Scheduler/Lyo.Scheduler/README.md",
  "dependencies": [
    {
      "name": "Lyo.Common",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.DateAndTime",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Exceptions",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Metrics",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Schedule.Models",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Microsoft.Extensions.Logging.Abstractions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "direct",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.DependencyInjection.Abstractions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.Options.ConfigurationExtensions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "System.Memory",
      "kind": "package",
      "version": "4.6.3",
      "tags": [
        "transitive",
        "microsoft",
        "netstandard2.0"
      ]
    },
    {
      "name": "System.Text.Json",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft",
        "netstandard2.0"
      ]
    }
  ],
  "targetFrameworks": [
    "netstandard2.0",
    "net10.0"
  ]
}
