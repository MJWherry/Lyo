{
  "id": "Lyo.FFmpeg",
  "name": "Lyo.FFmpeg",
  "area": "Data",
  "tagline": "CliWrap wrapper around ffmpeg, ffprobe, and ffplay for convert, play, and stream of audio and video.",
  "description": "CliWrap wrapper around `ffmpeg` / `ffprobe` / `ffplay` for [`Lyo.Media.Models`](../Lyo.Media.Models/README.md): sibling IAudio* and IVideo* contracts. Convert includes transcode and compress. Stream is StartConvertAsync (stdout readable while ffmpeg runs). Play is local ffplay. Images stay [`IImageService`](../../Images/Lyo.Images/README.md). This package does not reference FileStorage, Blazor, or Discord. One instance is safe for concurrent convert/probe/play (one process per call; no process-wide lock).",
  "features": [
    "**Convert.** File-to-file transcode and compress (CRF, bitrate, preset). Pipe mode does not spool to a temp file. Audio and video facades share one runner.",
    "**Stream.** StartConvertAsync returns a session whose stdout is live. ForRawPcm for Discord-shaped PCM. ForStreamingVideo remuxes fragmented mp4; WebM is copy plus pipe with no movflags.",
    "**Play.** ffplay for audio and video. NoDisplay defaults true so headless hosts do not open a window; video window is opt-in.",
    "**Probe.** ffprobe JSON via TypeConversion. Video width/height/fps. Album art is not HasVideo."
  ],
  "examples": [
    {
      "title": "Probe and convert audio",
      "language": "csharp",
      "code": "using Lyo.FFmpeg;\nusing Lyo.FFmpeg.Models;\nusing Lyo.Media.Models;\nusing Microsoft.Extensions.DependencyInjection;\n\nvar services = new ServiceCollection();\nservices.AddFFmpegServices(o => {\n    o.FFmpegPath = \"/usr/bin/ffmpeg\";\n    o.FfprobePath = \"/usr/bin/ffprobe\";\n});\n\nawait using var sp = services.BuildServiceProvider();\nusing var scope = sp.CreateScope();\n\nvar prober = scope.ServiceProvider.GetRequiredService<IAudioProber>();\nvar probe = await prober.ProbeAsync(\"clip.mp3\");\nif (probe.IsSuccess) Console.WriteLine($\"{probe.Data!.DurationSeconds}s {probe.Data.Codec}\");\n\nvar converter = scope.ServiceProvider.GetRequiredService<IAudioConverter>();\nawait converter.ConvertFileToFileAsync(\"clip.mp3\", \"clip.wav\", AudioConversionOptions.ForAudio());"
    },
    {
      "title": "Compress video then remux for a browser pipe",
      "language": "csharp",
      "code": "var compress = VideoConversionOptions.ForCompressVideo();\nawait video.ConvertFileToFileAsync(\"in.mov\", \"out.mp4\", compress);\n\nvar stream = VideoConversionOptions.ForStreamingVideo();\nawait using var session = (await video.StartConvertAsync(\"out.mp4\", stream)).Data!;\nawait session.StandardOutput.CopyToAsync(httpResponseBody);"
    },
    {
      "title": "Discord: PCM frames (host wiring, not a package ref)",
      "language": "csharp",
      "code": "var pcm = AudioConversionOptions.ForRawPcm();\nawait using var session = (await audio.StartConvertAsync(fileOrUrl, pcm)).Data!;\nvar frames = new PcmFrameReader(session.StandardOutput);\nawait foreach (var frame in frames.ReadFramesAsync(ct))\n    await sendVoicePacket(frame);"
    },
    {
      "title": "FileStorage input (host wiring, not a package ref)",
      "language": "text",
      "code": "Plaintext object: pass GetPreSignedReadUrlAsync to ProbeAsync / ConvertFileToFileAsync / PlayAsync as -i (ffmpeg can HTTP range-seek).\nEncrypted or compressed object: GetFileStreamAsync into StartConvertAsync(Stream) or ConvertStreamTo* with IoMode.Pipe. Never GetFileAsync (full byte[]). Never point ffmpeg at a presigned URL of ciphertext."
    },
    {
      "title": "Blazor: skip FFmpeg when the browser can play the file",
      "language": "text",
      "code": "If the stored mp3/mp4 is already playable, give the browser a URL. Use ForStreamingVideo (copy + fragmented mp4) or ForCompressVideo plus FragmentedOutput when you must remux or shrink. Ordinary non-fragmented mp4 is file-to-file only. WebM streaming is copy + pipe with no movflags."
    },
    {
      "title": "appsettings sample",
      "language": "json",
      "code": "{\n  \"FFmpegOptions\": {\n    \"FFmpegPath\": \"/usr/bin/ffmpeg\",\n    \"FfprobePath\": \"/usr/bin/ffprobe\",\n    \"FfplayPath\": null,\n    \"DefaultCodec\": \"pcm_s16le\",\n    \"DefaultSampleRate\": 44100,\n    \"DefaultChannels\": 2,\n    \"DefaultFormat\": \"wav\",\n    \"DefaultOverwrite\": true,\n    \"EnableMetrics\": false,\n    \"SuppressFfplayOutput\": true,\n    \"ProcessOutputMode\": \"Suppress\",\n    \"ProcessTimeout\": \"00:05:00\",\n    \"GlobalArguments\": [\"-hide_banner\", \"-loglevel\", \"warning\"]\n  }\n}"
    },
    {
      "title": "AddFFmpegServicesFromConfiguration",
      "language": "csharp",
      "code": "services.AddFFmpegServicesFromConfiguration(builder.Configuration);"
    },
    {
      "title": "Build a command by hand",
      "language": "csharp",
      "code": "var cmd = FFmpegCommandBuilder.New()\n    .WithDefaults(options)\n    .WithInput(\"clip.mp3\")\n    .WithOutput(\"clip.wav\")\n    .WithAudioCodec(AudioEncoder.PcmS16le)\n    .WithSampleRate(44100)\n    .WithChannels(2)\n    .WithFormat(MediaContainer.Wav)\n    .DropVideo()\n    .Overwrite()\n    .Build();"
    }
  ],
  "sections": [
    {
      "type": "table",
      "title": "Public types",
      "headers": [
        "Type",
        "Description"
      ],
      "rows": [
        [
          "FFmpegAudioConverter / FFmpegVideoConverter",
          "IAudioConverter and IVideoConverter on ffmpeg. Shared runner. Concurrent converts on one instance are two processes."
        ],
        [
          "FFmpegAudioProber / FFmpegVideoProber",
          "IAudioProber and IVideoProber on ffprobe."
        ],
        [
          "FFmpegAudioPlayer / FFmpegVideoPlayer",
          "IAudioPlayer and IVideoPlayer on ffplay. NoDisplay defaults true."
        ],
        [
          "FFmpegCommandBuilder",
          "Fluent argv builder. Do not reuse across threads. Maps MediaContainer.Format and encoder Id onto ffmpeg flags (h264 becomes libx264). WithFormat emits -f. NoOverwrite emits -n."
        ],
        [
          "Extensions",
          "AddFFmpegServices(), AddFFmpegServices(Action), AddFFmpegServices(FFmpegOptions), AddFFmpegServicesFromConfiguration."
        ]
      ],
      "trail": "Every AddFFmpegServices* overload registers audio and video converter, prober, and player as scoped services (TryAddScoped) and exposes them as both the concrete type and their interfaces. A Discord host can take IAudioConverter only."
    },
    {
      "type": "list",
      "title": "Notes",
      "items": [
        "`ffmpeg`, `ffprobe`, and (for playback) `ffplay` must be on PATH or set via FFmpegOptions. Binaries are not checked at DI time.",
        "Failures use Constants.Errors (FFmpegError, FFprobeError, FfplayError, ProbeParseError).",
        "ForAudio applies pcm/wav defaults. A bare AudioConversionOptions does not force pcm. Audio converts always emit -vn.",
        "Set Crf or VideoBitRate, not both. CopyCodecs remuxes without re-encode. Compress is an explicit factory. When H.264 is missing, hosts can pass VideoEncoder.Mpeg4.",
        "StartConvertAsync does not apply ProcessTimeout. Cancel with the token or by disposing the session.",
        "IoMode.Pipe uses pipe:0/pipe:1. Muxed mp4 on a pipe needs FragmentedOutput (ForStreamingVideo default). WebM streaming is copy + pipe, no movflags. Convert*ToBytesAsync is for tiny clips.",
        "KnownDuration is only for MediaProgress.Percentage. IProgress callbacks fire on the CliWrap thread; hosts that mutate UI must synchronize.",
        "Encrypted FileStorage objects: stream via GetFileStreamAsync. Presigned GET is ciphertext when the file is encrypted."
      ]
    }
  ],
  "links": [],
  "readmePath": "Lyo.Net/Data/Media/Lyo.FFmpeg/README.md",
  "dependencies": [
    {
      "name": "Lyo.Common.Core",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Exceptions",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.FFmpeg.Models",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Media.Models",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Metrics",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Result",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "CliWrap",
      "kind": "package",
      "version": "3.10.2",
      "tags": [
        "direct",
        "third-party"
      ]
    },
    {
      "name": "Microsoft.Extensions.Configuration.Binder",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "direct",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.DependencyInjection.Abstractions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "direct",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.Logging.Abstractions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "direct",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.Options",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "direct",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Bcl.AsyncInterfaces",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft",
        "netstandard2.0"
      ]
    },
    {
      "name": "System.Memory",
      "kind": "package",
      "version": "4.6.3",
      "tags": [
        "transitive",
        "microsoft",
        "netstandard2.0"
      ]
    },
    {
      "name": "System.Text.Json",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft",
        "netstandard2.0"
      ]
    }
  ],
  "targetFrameworks": [
    "net10.0"
  ]
}
