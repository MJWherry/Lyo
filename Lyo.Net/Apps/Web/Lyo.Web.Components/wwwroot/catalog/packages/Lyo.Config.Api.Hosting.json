{
  "id": "Lyo.Config.Api.Hosting",
  "name": "Lyo.Config.Api.Hosting",
  "area": "Apps",
  "tagline": "Connects `IConfigApiClient` (`Lyo.Config.Api.Client`) to `Microsoft.Extensions.Options` and `Microsoft.Extensions.DependencyInjection`. A `BackgroundService` polls a shared `ResolvedConfigRecord` ledger.",
  "description": "Connects `IConfigApiClient` ([`Lyo.Config.Api.Client`](../Lyo.Config.Api.Client/README.md)) to `Microsoft.Extensions.Options` and `Microsoft.Extensions.DependencyInjection`. A `BackgroundService` keeps a shared `ResolvedConfigRecord` ledger (`304` polling + ETags). Then one definition-key JSON blob binds each `IOptionsMonitor<TOptions>`.\n\nPrefer `IOptionsMonitor<TOptions>.CurrentValue` (or `OnChange`) for values that reload at runtime. `IOptions<TOptions>` is not registered here and would not observe remote updates anyway.",
  "features": [],
  "examples": [
    {
      "title": "Order of registration",
      "language": "csharp",
      "code": "using Lyo.Config.Api.Client;\nusing Lyo.Config.Api.Hosting;\n\n// 1 — HTTP client (`BaseUrl`, optional `ApiKey`)\nservices.AddConfigApiClientFromConfiguration(configuration);\n\n// 2 — ledger + polling (binds configuration section defaults below)\nservices.AddConfigApiPolling(configuration);\n\n// 3 — one registrant per POCO keyed by Config API definition Key\nservices.AddConfigApiOptions<MyFeatureOptions>(\n    definitionKey: \"myFeature\",\n    missingDefinitionKeyBehavior: ConfigApiMissingDefinitionKeyBehavior.Throw);"
    },
    {
      "title": "Configuration",
      "language": "json",
      "code": "{\n  \"ConfigApi\": {\n    \"BaseUrl\": \"http://localhost:5088/\",\n    \"ApiKey\": \"\"\n  },\n  \"ConfigApiPolling\": {\n    \"Enabled\": true,\n    \"AppKind\": \"worker\",\n    \"AppId\": \"image-processor\",\n    \"DelayWhenNotModified\": \"00:00:15\",\n    \"StartupTimeout\": \"00:05:00\",\n    \"RequireSuccessOnStartup\": true\n  }\n}"
    }
  ],
  "sections": [
    {
      "type": "paragraph",
      "title": "Order of registration",
      "text": "From your worker/API host, reference the project `Lyo.Config.Api.Hosting` (`Microsoft.Extensions.Hosting` is assumed)."
    },
    {
      "type": "list",
      "title": "Configuration",
      "items": [
        "`StartupTimeout`. Omit or `null` to wait indefinitely for the first **200**.",
        "`RequireSuccessOnStartup`. `false` allows the host to start after `StartupTimeout` even if no snapshot arrived (ledger stays empty unless you later reload manually; prefer keeping `true` unless you tolerate cold-start without remote config)."
      ]
    },
    {
      "type": "table",
      "title": "Polling on versus off",
      "headers": [
        "`Enabled`",
        "`ConfigApiPollingHostedService` behavior",
        "`ConfigApiResolvedLedger.Current` at startup",
        "Behavior of `IOptionsMonitor<T>` from `AddConfigApiOptions<T>`"
      ],
      "rows": [
        [
          "`true`",
          "`StartAsync` validates `AppKind` / `AppId` and **blocks until the first 200** (or `StartupTimeout` if set). `ExecuteAsync` then loops `ResolveForAppAsync` with the latest ETag and `DelayWhenNotModified`. Errors retry after fixed back-offs and are logged.",
          "Populated with the first successful payload after `StartAsync` returns.",
          "`CurrentValue` returns the bound `TOptions`. `OnChange` fires after each ledger swap."
        ],
        [
          "`false`",
          "`StartAsync` skips validation and the first probe; `ExecuteAsync` returns immediately. The hosted service stays in the DI container but does not touch the network. **No ledger updates from the network ever occur.** Some other code path (typically tests) may still call `ConfigApiResolvedLedger.SetResolved` manually.",
          "`null` unless something else calls `SetResolved` directly.",
          "Materialization sees a `null` ledger and falls back to `ConfigApiMissingDefinitionKeyBehavior`: `Throw` raises `InvalidOperationException`; `UseDefaultInstance` returns `new TOptions()`."
        ]
      ],
      "lead": "The entire background poller is gated by `ConfigApiPollingOptions.Enabled` (defaults to `true`):",
      "trail": "> In production, the polling service is the **only** writer to `ConfigApiResolvedLedger`. With `Enabled == false`, every options monitor is effectively driven by\n> `missingDefinitionKeyBehavior`. Use that mode for test hosts or for services that consume config exclusively via REST."
    },
    {
      "type": "table",
      "title": "`ConfigApiResolvedLedger`",
      "headers": [
        "Member",
        "Purpose"
      ],
      "rows": [
        [
          "`ResolvedConfigRecord? Current`",
          "Latest payload. `null` until the first successful resolve. Thread-safe getter (lock-protected)."
        ],
        [
          "`string? CurrentEtag`",
          "Opaque ETag from the last successful 200 response. Passed back to the server as `If-None-Match` on every subsequent probe."
        ],
        [
          "`IChangeToken GetReloadToken()`",
          "Returns a `CancellationChangeToken` invalidated on the next swap. Used by `ConfigApiOptionsMonitor<T>` to rebuild its cached `TOptions`."
        ],
        [
          "`void SetResolved(ResolvedConfigRecord resolved, string? etag)`",
          "Atomically updates `Current` + `CurrentEtag`, cancels the previous reload token (notifying every subscriber), and disposes the old `CancellationTokenSource`."
        ]
      ],
      "lead": "The shared in-process snapshot that ties the background poller to all `IOptionsMonitor<T>` instances is [`ConfigApiResolvedLedger`](./ConfigApiResolvedLedger.cs). It is\nregistered as a **singleton** by `AddConfigApiPolling` (and also `TryAddSingleton`-ed by `AddConfigApiOptions<T>`, so you can register options without polling).",
      "trail": "`GetReloadToken` is subscribed via `ChangeToken.OnChange` by `ConfigApiOptionsMonitor<T>`, so every successful ledger swap triggers re-materialization of *all* registered options\ntypes in lock-step."
    },
    {
      "type": "table",
      "title": "What happens when a definition key is missing",
      "headers": [
        "Condition",
        "`Throw` (default)",
        "`UseDefaultInstance`"
      ],
      "rows": [
        [
          "Ledger is empty (`Current == null`).",
          "`InvalidOperationException(\"No Config API snapshot is available yet (ledger empty). …\")`",
          "`new TOptions()`"
        ],
        [
          "Definition key absent from the resolved payload, or its JSON value is `null`.",
          "`InvalidOperationException(\"Definition key '<key>' is missing from resolved Config API payload …\")`",
          "`new TOptions()`"
        ],
        [
          "Key present, JSON not assignable to `TOptions` (deserialize returns `null`).",
          "`InvalidOperationException(\"JSON for definition key '<key>' did not deserialize to <TOptions>.\")`",
          "`new TOptions()`"
        ]
      ],
      "lead": "`ConfigApiResolvedLedger.Current.TryGetValue(definitionKey, …)` then `configValue.GetValue<T>(ConfigJsonSerializerOptions.Default)` is the materialization path. Failure modes\nare controlled by the `ConfigApiMissingDefinitionKeyBehavior` selected at registration:",
      "trail": "Pick `UseDefaultInstance` for features that may have no bindings yet; keep `Throw` for required configuration so misconfiguration fails fast at startup."
    },
    {
      "type": "list",
      "title": "Limitations",
      "items": [
        "**No named options.** `ConfigApiOptionsMonitor<T>` ignores the `name` argument on `IOptionsMonitor<T>.Get(string?)`: requests for `Options.DefaultName` (or `null` / empty) return the cached `CurrentValue`. Anything else throws `InvalidOperationException`. There is no way to map several names onto different definition keys through this monitor.",
        "**One definition key per `TOptions` type per host.** `AddConfigApiOptions<TOptions>(definitionKey, …)` registers an unkeyed singleton `IOptionsMonitor<TOptions>`. Calling it twice with the same `TOptions` and different `definitionKey` values **replaces** the prior registration (last call wins); you cannot bind one POCO to two definition keys in the same host. Use distinct `TOptions` types (or sub-records) for each definition key.",
        "**`Enabled = false` leaves the ledger empty.** Without polling, `ConfigApiResolvedLedger.Current` stays `null` unless something else calls `SetResolved` directly. Options monitors then fall back to `ConfigApiMissingDefinitionKeyBehavior` semantics, see the table above.",
        "**`IOptions<T>` and `IOptionsSnapshot<T>` are not registered** by this package. Inject `IOptionsMonitor<T>` (or, in scoped consumers, `IOptionsSnapshot<T>` if you register your own adapter) so changes from the polling service propagate."
      ]
    },
    {
      "type": "markdown",
      "title": "What happens at runtime",
      "body": "```mermaid\nflowchart LR\n    HS[BackgroundService_poll] --> Ledger[Resolved_ledger_ETag]\n    Ledger --> Mon[OptionsMonitor_builder]\n```\n\n1. The hosted service probes `IConfigApiClient.ResolveForAppAsync` with `AppKind`/`AppId` and `If-None-Match` from `ConfigApiResolvedLedger.CurrentEtag`.\n2. On **200**, the ledger swaps in the new `ResolvedConfigRecord` and invalidates `IChangeToken` listeners.\n3. Each `ConfigApiOptionsMonitor<T>` reacts by re-materializing `T` via `ResolvedConfigRecord.TryGetValue(definitionKey, …)` + JSON deserialize (same `ConfigJsonSerializerOptions.Default` semantics as `GetValue<T>` elsewhere).\n\nREST paths remain in [`../Lyo.Config.Api/README.md`](../Lyo.Config.Api/README.md). Resolve outcomes (`ConfigResolveOutcome`) are in [`Lyo.Config.Api.Models`](../Lyo.Config.Api.Models/README.md); HTTP registration and `ConfigPolling` in [`Lyo.Config.Api.Client`](../Lyo.Config.Api.Client/README.md)."
    }
  ],
  "links": [],
  "readmePath": "Lyo.Net/Apps/Config/Lyo.Config.Api.Hosting/README.md",
  "dependencies": [
    {
      "name": "Lyo.Config.Api.Client",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Microsoft.Extensions.Hosting",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "direct",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.Options.ConfigurationExtensions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "direct",
        "microsoft"
      ]
    },
    {
      "name": "Lyo.Api.Client",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Api.Models",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Common.Core",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Common.Json",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Common.Metadata",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Config",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Config.Api.Models",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Configuration",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.DateAndTime",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Diagnostic",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.EntityReference.Models",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Exceptions",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Hashing",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Http.Client",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Metrics",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.PackageMetadata",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Parameters",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Query.Evaluation",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Query.Models",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Result",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Validation",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Validation.Models",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "AngleSharp",
      "kind": "package",
      "version": "1.5.0",
      "tags": [
        "transitive",
        "third-party"
      ]
    },
    {
      "name": "Microsoft.Bcl.AsyncInterfaces",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft",
        "netstandard2.0"
      ]
    },
    {
      "name": "Microsoft.Extensions.Configuration.Binder",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.DependencyInjection.Abstractions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.Http",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.Logging.Abstractions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "System.ComponentModel.Annotations",
      "kind": "package",
      "version": "5.0.0",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "System.IO.Hashing",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft",
        "net10.0"
      ]
    },
    {
      "name": "System.Memory",
      "kind": "package",
      "version": "4.6.3",
      "tags": [
        "transitive",
        "microsoft",
        "netstandard2.0"
      ]
    },
    {
      "name": "System.Text.Json",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft",
        "netstandard2.0"
      ]
    },
    {
      "name": "System.Threading.RateLimiting",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "System.Threading.Tasks.Extensions",
      "kind": "package",
      "version": "4.6.3",
      "tags": [
        "transitive",
        "microsoft"
      ]
    }
  ],
  "targetFrameworks": [
    "netstandard2.0",
    "net10.0"
  ]
}
