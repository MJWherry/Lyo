{
  "id": "Lyo.Job.Scheduler",
  "name": "Lyo.Job.Scheduler",
  "area": "Apps",
  "tagline": "A hosted `JobScheduler` polls the Job API for enabled definitions, evaluates schedules (blackout calendars, misfire catch-up, per-schedule time zones), and creates job runs via `IApiClient`.",
  "description": "A hosted `JobScheduler` polls the Job API for enabled definitions, evaluates schedules (with blackout calendars, misfire catch-up, and per-schedule time zones), creates job runs\nvia `IApiClient`, listens to `IJobEventPublisher` for definition updates and run completions, fires triggers, schedules retries with linear or **exponential backoff**, aggregates\nbatch parent progress, trips/resets per-definition circuit breakers, and publishes failure/circuit-breaker alerts.\n\nOptional `JobWorkflowEngine` advances multi-step workflow runs when constituent job runs finish. Step runs are created with dispatch suppressed and only published after the run\nis linked to its workflow run step, so a fast worker cannot finish a step run before the engine knows which step it belongs to. `JobScheduler` completion-message failures on\n`job.run.complete` use a bounded counted requeue (max 3, like `QueueWorkerBase`) instead of requeueing forever, so a poison message cannot loop indefinitely. `JobWorkflowEngine`\nuses the same pattern (max 5).\n\nSafe for multi-instance hosts: duplicate slot creates return the existing run. The due-slot cursor uses the later of last success and this schedule's last attempted slot so a failed scheduled run is not re-POSTed every check.",
  "features": [],
  "examples": [
    {
      "title": "Register in DI",
      "language": "csharp",
      "code": "// IMqService (RabbitMQ) + Job.Client publisher — not Lyo.Job.Postgres\nservices.AddJobClient(sp => sp.GetRequiredService<IApiClient>());\nservices.AddMqJobEventPublisherFromConfiguration(configuration);\n// or: services.AddMqJobEventPublisher();\n\nservices.AddJobScheduler(new JobSchedulerOptions {\n    ApiBaseUrl = \"https://api.example.com\",\n    TimeZone = TimeZoneInfo.FindSystemTimeZoneById(\"America/New_York\"),\n    DefinitionRefreshIntervalSeconds = 30,\n    ScheduleCheckIntervalSeconds = 10,\n    EnableMisfireCatchUp = true,\n    MisfireLookbackMinutes = 1440,\n    CreatedBy = \"Scheduler\",\n});\n\n// Or bind from the \"JobScheduler\" configuration section:\nservices.AddJobScheduler();\n\n// Optional workflow orchestration (section JobWorkflowEngine):\nservices.AddJobWorkflowEngine(new JobWorkflowEngineOptions {\n    ApiBaseUrl = \"https://api.example.com\",\n    CreatedBy = \"WorkflowEngine\",\n});\n// services.AddJobWorkflowEngine();"
    },
    {
      "title": "`IJobScheduler` methods",
      "language": "csharp",
      "code": "public interface IJobScheduler\n{\n    bool IsRunning { get; }\n    Task RefreshDefinitionsAsync(CancellationToken ct = default);\n    Task CheckSchedulesAsync(CancellationToken ct = default);\n}"
    }
  ],
  "sections": [
    {
      "type": "paragraph",
      "title": "Registration",
      "text": "Needs `IApiClient`, `IFormatterService`, and `IJobEventPublisher`. For scheduler/worker hosts register the non-EF publisher via `Lyo.Job.Client.AddMqJobEventPublisher()` / `AddMqJobEventPublisherFromConfiguration()` (`IMqService` + optional `IJobClient`). Do **not** use `Lyo.Job.Postgres.AddMqJobEventPublisher*`. That path pulls EF. `IMqService` and `IMetrics` are optional for the scheduler itself (MQ is required for the publisher)."
    },
    {
      "type": "table",
      "title": "`JobSchedulerOptions` (`JobSchedulerOptions.SectionName` = `\"JobScheduler\"`)",
      "headers": [
        "Property",
        "Type",
        "Default",
        "Notes"
      ],
      "rows": [
        [
          "`ApiBaseUrl`",
          "`string`",
          "_required_",
          "Base URL of the Job API."
        ],
        [
          "`TimeZone`",
          "`TimeZoneInfo?`",
          "`null` (UTC)",
          "Fallback zone when a schedule has no `TimeZoneId`."
        ],
        [
          "`DefinitionRefreshIntervalSeconds`",
          "`int`",
          "`30`",
          "How often definitions + calendars refresh."
        ],
        [
          "`ScheduleCheckIntervalSeconds`",
          "`int`",
          "`10`",
          "How often schedules are evaluated."
        ],
        [
          "`CreatedBy`",
          "`string`",
          "`\"Scheduler\"`",
          "Written onto runs the scheduler creates."
        ],
        [
          "`EnableMisfireCatchUp`",
          "`bool`",
          "`true`",
          "Global default; a schedule's `MisfirePolicy` can override."
        ],
        [
          "`MisfireLookbackMinutes`",
          "`int`",
          "`1440`",
          "Oldest missed slots still eligible for `RunOnce` catch-up."
        ]
      ],
      "trail": "`TimeZone` binds from configuration using Windows / IANA identifiers (e.g. `\"UTC\"`, `\"America/New_York\"`)."
    },
    {
      "type": "table",
      "title": "`JobWorkflowEngineOptions` (`JobWorkflowEngineOptions.SectionName` = `\"JobWorkflowEngine\"`)",
      "headers": [
        "Property",
        "Default",
        "Notes"
      ],
      "rows": [
        [
          "`ApiBaseUrl`",
          "_required_",
          "Job API base URL."
        ],
        [
          "`CreatedBy`",
          "`\"WorkflowEngine\"`",
          "Written onto workflow-run steps."
        ]
      ]
    },
    {
      "type": "table",
      "title": "Metrics (`job.scheduler.*`)",
      "headers": [
        "Metric",
        "Description"
      ],
      "rows": [
        [
          "`job.scheduler.definitions.loaded`",
          "Count after a refresh"
        ],
        [
          "`job.scheduler.refresh.duration`",
          "Timer for refresh"
        ],
        [
          "`job.scheduler.refresh.error`",
          "Failed refreshes"
        ],
        [
          "`job.scheduler.check.duration`",
          "Timer for schedule checks"
        ],
        [
          "`job.scheduler.check.error`",
          "Failed checks"
        ],
        [
          "`job.scheduler.runs.created`",
          "Created runs"
        ],
        [
          "`job.scheduler.runs.create.failed`",
          "Failed API creates"
        ],
        [
          "`job.scheduler.slot.conflicts`",
          "Benign duplicate-slot conflicts"
        ],
        [
          "`job.scheduler.triggers.fired`",
          "Runs driven by triggers"
        ],
        [
          "`job.scheduler.retries.scheduled`",
          "Retry runs after failure"
        ],
        [
          "`job.scheduler.circuit_breaker.tripped`",
          "Disabled definitions"
        ],
        [
          "`job.scheduler.misfires.caught_up`",
          "`RunOnce` misfire runs"
        ],
        [
          "`job.scheduler.misfires.skipped`",
          "Skipped missed slots"
        ]
      ]
    },
    {
      "type": "markdown",
      "title": "What happens at runtime",
      "body": "```mermaid\nflowchart TB\n    start[Startup] --> setup[EventPublisher.SetupAsync]\n    setup --> refresh[Refresh definitions + calendars]\n    refresh --> loops[Periodic refresh + schedule check]\n    loops --> eval[Evaluate next slot]\n    eval --> cal{Blackout calendar?}\n    cal -->|Skip| skip[Skip slot]\n    cal -->|Defer| defer[Shift to window end]\n    cal -->|OK| create[POST JobRunReq]\n    create --> mq[PublishRunCreated priority]\n    finish[job.run.complete] --> retry{Failure + retries?}\n    retry -->|yes| backoff[JobRetryBackoff delay]\n    retry -->|no| trig[Fire triggers]\n    finish --> batch[Update parent batch progress]\n    finish --> alert[PublishAlert on failure/CB]\n```\n\n1. **Startup**. `SetupAsync`, subscribe to definition-change and run-completion queues, initial refresh + misfire pass, start periodic timers.\n2. **Definition refresh**. Enabled definitions load with schedules, triggers, parallel restrictions; last-run snapshots come in one batch via\n `POST Job/Definition/LatestRuns` (falls back to per-definition queries when the endpoint is unavailable); caches them as `JobInfo`.\n3. **Calendar refresh**. `JobBlackoutCalendar` + windows load for blackout evaluation (`Lyo.Job.Models.JobBlackoutCalendarEvaluator`: weekday, dated range, calendar day-of-month, or holiday slug; `Skip` or `Defer`).\n4. **Schedule check**. Skips when a run is already `Running`/`Queued`; respects parallel restrictions; applies misfire policy; creates runs with `ScheduledSlotUtc` for idempotency; skips a slot this process already created or that last-run/last-failed already owns, using the later of last success and this schedule's last attempted slot as the cursor.\n5. **Misfire catch-up**. When `EnableMisfireCatchUp` and schedule `MisfirePolicy == RunOnce`, creates one run for the most recent missed slot within lookback.\n6. **Run completion**. Updates cached last-run pointers; circuit breaker on consecutive failures; retries via `JobRetryBackoff.ComputeBackoffSeconds` (`Exponential` or `Linear`);\n batch parent progress; failure alerts when `AlertOnFailure` and `AlertAfterConsecutiveFailures` threshold is met. Timeouts (`JobRunResult.Timeout`, published by the maintenance\n dead-job scan) count as failures for retry and circuit-breaker purposes.\n7. **Triggers**. When `AllowTriggers`, matching trigger definitions spawn new runs.\n8. **Definition updates**. Per-definition cache refresh; disabled definitions removed; a 404 evicts the definition from the cache (deleted definitions stop producing doomed runs)."
    },
    {
      "type": "paragraph",
      "title": "Templates on string parameters",
      "text": "When creating a run, Formatter and String parameter values (schedule, definition, and trigger) are formatted with `IFormatterService` against `Schedule`, `Definition`, last-run snapshots, and related objects. Pick type `formatter` in the catalog for Guid/DateTime templates (`{-}`, `{DateTime.UtcNow}`). Write `{Definition.Name}` — unresolved tokens such as `{client.contact.emailAddress}` stay in the value until the worker `AddContext`s them. Xml and Json values are not formatted. Legacy `{{Definition.Name}}` is unwrapped to `{Definition.Name}` in the scheduler only so existing schedules keep working; `{{` is not the placeholder standard (SmartFormat uses it as a literal brace)."
    },
    {
      "type": "markdown",
      "title": "Backoff on retry",
      "body": "A failed run with retries remaining produces exactly **one** dispatch after the computed delay. The retry run is always created immediately (as `Queued`), and one of three dispatch\npaths applies:\n\n- **Delayed MQ available** (`IMqService is IDelayedMqService`): the create sets `SuppressDispatch = true` and the scheduler publishes a delayed `QueueMessageEnvelope` to the worker\n queue; the envelope is the sole dispatch.\n- **No delayed MQ**: the create sets `ScheduledSlotUtc` to the due time, which suppresses the immediate publish; the maintenance service's stuck-queued recovery dispatches the run\n once the slot comes due.\n- **No backoff** (`backoffSeconds == 0`): the API's immediate publish dispatches as usual.\n\nIf a delayed envelope is lost (scheduler crash, broker restart), the stuck-queued recovery also re-publishes it once the run has sat untouched past the threshold. The run is never\nsilently stranded. Retry creation carries an idempotency key (`retry:{failedRunId}:{attempt}`), so duplicate completion deliveries or two scheduler instances processing the same\ncompletion cannot create duplicate retries. Trigger firing is deduplicated the same way (`trigger:{triggerId}:{completedRunId}`)."
    },
    {
      "type": "paragraph",
      "title": "Completion across instances",
      "text": "`job.run.complete` is a competing-consumer queue: each completion is processed by exactly one scheduler instance. Correctness across instances relies on idempotency keys (triggers, retries) and the `(JobScheduleId, ScheduledSlotUtc)` unique constraint (scheduled slots), not on instance affinity. Completion processing takes the definition lock before mutating the in-memory `JobInfo` cache, so concurrent refreshes cannot interleave with circuit-breaker counter updates. A 404 from `GET Job/Run/{id}` is acked (run not found). Other processing failures (including HTTP 500) are retried with a counted envelope up to 3 times, then dropped. Never nack-requeued forever."
    },
    {
      "type": "paragraph",
      "title": "Restrictions on parallelism",
      "text": "When a definition has `JobParallelRestriction` rows, `CheckSchedulesAsync` skips creating a run if any restricted definition (cross-definition or same link) already has a `Running` or `Queued` run. This complements per-definition `MaxConcurrentRuns` enforced in `JobService`."
    },
    {
      "type": "markdown",
      "title": "`IJobScheduler` methods",
      "body": "`JobScheduler` implements `Lyo.Health.IHealth` (`HealthCheckName = \"job-scheduler\"`) with `loaded_job_count`, `is_running`, `last_schedule_check_utc`, and\n`last_definitions_refresh_utc`."
    }
  ],
  "links": [],
  "readmePath": "Lyo.Net/Apps/Job/Lyo.Job.Scheduler/README.md",
  "dependencies": [
    {
      "name": "Lyo.Api.Client",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Api.Models",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Common.Metadata",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Formatter",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Health",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Http.Client",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Job.Models",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.MessageQueue",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Metrics",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Query.Models",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Scheduler",
      "kind": "lyo",
      "tags": [
        "direct",
        "lyo"
      ]
    },
    {
      "name": "Microsoft.Extensions.Hosting.Abstractions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "direct",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.Options.ConfigurationExtensions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "direct",
        "microsoft"
      ]
    },
    {
      "name": "Lyo.Common.Core",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Common.Json",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Configuration",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.DateAndTime",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Diagnostic",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Exceptions",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Hashing",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.PackageMetadata",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Parameters",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Result",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "Lyo.Schedule.Models",
      "kind": "lyo",
      "tags": [
        "transitive",
        "lyo"
      ]
    },
    {
      "name": "AngleSharp",
      "kind": "package",
      "version": "1.5.0",
      "tags": [
        "transitive",
        "third-party"
      ]
    },
    {
      "name": "DynamicExpresso.Core",
      "kind": "package",
      "version": "2.19.3",
      "tags": [
        "transitive",
        "third-party"
      ]
    },
    {
      "name": "Microsoft.Bcl.AsyncInterfaces",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft",
        "netstandard2.0"
      ]
    },
    {
      "name": "Microsoft.Extensions.Configuration.Binder",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.DependencyInjection.Abstractions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.Http",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "Microsoft.Extensions.Logging.Abstractions",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "SmartFormat.NET",
      "kind": "package",
      "version": "3.6.1",
      "tags": [
        "transitive",
        "third-party"
      ]
    },
    {
      "name": "System.Diagnostics.DiagnosticSource",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft",
        "netstandard2.0"
      ]
    },
    {
      "name": "System.IO.Hashing",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft",
        "net10.0"
      ]
    },
    {
      "name": "System.Memory",
      "kind": "package",
      "version": "4.6.3",
      "tags": [
        "transitive",
        "microsoft",
        "netstandard2.0"
      ]
    },
    {
      "name": "System.Text.Json",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft",
        "netstandard2.0"
      ]
    },
    {
      "name": "System.Threading.RateLimiting",
      "kind": "package",
      "version": "10.0.5",
      "tags": [
        "transitive",
        "microsoft"
      ]
    },
    {
      "name": "System.Threading.Tasks.Extensions",
      "kind": "package",
      "version": "4.6.3",
      "tags": [
        "transitive",
        "microsoft"
      ]
    }
  ],
  "targetFrameworks": [
    "net10.0"
  ]
}
