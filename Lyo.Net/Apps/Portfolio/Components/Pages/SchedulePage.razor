@page "/schedule"

<PageTitle>Schedule Workbench — Portfolio</PageTitle>

<MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="mt-4 mb-6">
    <Lyo.Schedule.Web.Components.ScheduleWorkbench/>
</MudContainer>

@code {

    [CascadingParameter]
    private MainLayout? Layout { get; set; }

    protected override void OnInitialized() => Layout?.SetPageTitle("Schedule Workbench");

}
