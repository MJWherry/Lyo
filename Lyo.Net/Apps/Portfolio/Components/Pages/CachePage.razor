@page "/cache"

<PageTitle>Cache Workbench — Portfolio</PageTitle>

<MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="mt-4 mb-6">
    <CacheWorkbench/>
</MudContainer>

@code {

    [CascadingParameter]
    private MainLayout? Layout { get; set; }

    protected override void OnInitialized() => Layout?.SetPageTitle("Cache Workbench");

}
