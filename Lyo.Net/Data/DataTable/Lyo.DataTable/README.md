# Lyo.DataTable

Empty package placeholder reserving the `Lyo.DataTable` name. The runtime types (`DataTable`, `DataTableRow`, `DataTableBuilder`, cell types, HTML renderer) all live in [
`Lyo.DataTable.Models`](../Lyo.DataTable.Models/README.md).

If you need the data-table API, reference `Lyo.DataTable.Models` directly.

## Targeting

`netstandard2.0;net10.0`. No source files, no project references, no NuGet dependencies.