# Lyo.DataTable

Placeholder package that holds the `Lyo.DataTable` name only. Runtime types (`DataTable`, `DataTableRow`, `DataTableBuilder`, cell types, HTML renderer) ship in [`Lyo.DataTable.Models`](../Lyo.DataTable.Models/README.md).

Take a reference on `Lyo.DataTable.Models` itself when you want the data-table API.

## TFMs

`netstandard2.0;net10.0`. No source, no project references, and no NuGet packages.