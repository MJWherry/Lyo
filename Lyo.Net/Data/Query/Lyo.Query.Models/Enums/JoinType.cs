namespace Lyo.Query.Models.Enums;

/// <summary>SQL join kind for root <c>/Query</c> <see cref="Common.Request.JoinClause" /> (v1: Inner and Left only).</summary>
public enum JoinType
{
    Inner = 0,
    Left = 1
}