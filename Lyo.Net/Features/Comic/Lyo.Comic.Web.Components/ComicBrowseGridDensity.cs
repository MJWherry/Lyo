namespace Lyo.Comic.Web.Components;

/// <summary>Column layout density for the comic browse grid.</summary>
public enum ComicBrowseGridDensity
{
    /// <summary>One card per row on narrow viewports, four cards per row on desktop (≥960px).</summary>
    Large,

    /// <summary>Two cards per row on narrow viewports, eight cards per row on desktop (≥960px).</summary>
    Small
}
