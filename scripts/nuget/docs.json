{
  "id": "scripts.nuget",
  "name": "NuGet pack tooling",
  "tagline": "Discover, build, and pack selected Lyo.* packages with git change detection.",
  "description": "SDK-style `dotnet pack` for libraries under Lyo.Net. Packs only the projects you name or that changed. No ProjectReference walk. Pins Lyo package dependencies to last published versions. Fingerprints each project directory so unchanged packages are skipped. Refreshes tooling READMEs via tooling-docs before packing.",
  "commands": [
    {
      "cmd": "python3 scripts/nuget/build_nuget.py",
      "blurb": "Pack all Lyo.* packages as 1.0.0-preview (skips unchanged)"
    },
    {
      "cmd": "python3 scripts/nuget/build_nuget.py -v 2.0.0",
      "blurb": "Pack all packages as 2.0.0-preview"
    },
    {
      "cmd": "python3 scripts/nuget/build_nuget.py --release",
      "blurb": "Pack all packages as a release version (no preview label)"
    },
    {
      "cmd": "python3 scripts/nuget/build_nuget.py --release --changed-since",
      "blurb": "Release-pack projects whose directory changed since the latest git tag (or HEAD~1)"
    },
    {
      "cmd": "python3 scripts/nuget/build_nuget.py --release -v 1.0.0 Lyo.Encryption",
      "blurb": "Release-pack one package only (does not restamp Common)"
    },
    {
      "cmd": "python3 scripts/nuget/build_nuget.py -f Lyo.Encryption",
      "blurb": "Force rebuild of Encryption ignoring change detection"
    },
    {
      "cmd": "python3 scripts/nuget/ci_pack.py --scope named --packages Lyo.Encryption --version 1.2.0 --channel preview",
      "blurb": "CI helper: same pack with SemVer / preview suffix handling"
    },
    {
      "cmd": "python3 scripts/nuget/build_nuget.py --compile-only Lyo.Encryption",
      "blurb": "Build Encryption only; do not pack"
    },
    {
      "cmd": "python3 scripts/nuget/build_nuget.py --pack-only Lyo.Encryption",
      "blurb": "Pack Encryption without compiling (needs a prior compile)"
    },
    {
      "cmd": "python3 scripts/nuget/ci_push.py --source https://api.nuget.org/v3/index.json --api-key \"$NUGET_API_KEY\" artifacts/nuget",
      "blurb": "Push nupkgs one at a time; fail if all are duplicates or any push errors"
    },
    {
      "cmd": "python3 scripts/nuget/ci_tag.py --version 1.0.5",
      "blurb": "Create and push annotated tag v1.0.5 at HEAD (stable releases only)"
    },
    {
      "cmd": "python3 scripts/nuget/ci_summary.py --phase result --version 1.0.5 --branch main --job pack=success",
      "blurb": "Write a Markdown pipeline report (pack set, publish outcomes, skip reasons) to stdout and $GITHUB_STEP_SUMMARY"
    },
    {
      "cmd": "python3 scripts/nuget/unlist_nuget.py 1.0.5",
      "blurb": "Unlist 1.0.5 on nuget.org for every packable Lyo.* package that has that version"
    }
  ],
  "notes": [
    "Local packs append the SemVer prerelease label preview (1.0.0-preview). Use --release when publishing a stable version so the version is used as-is",
    "Selection is named patterns, --changed-since hits, or all packable Lyo.* projects. No upstream or downstream graph walk. Shared Directory.Build/Packages.props or Lyo.Net/assets/icon.{png,svg} changes select all packages",
    "Nupkg Lyo ProjectReferences are pinned to last published (.build-state, nuget.org, GitHub Packages) unless that project is also in the pack set",
    "Subset packs compile those pinned ProjectReferences at the last published assembly version. A global /p:Version would stamp Fusion 1.0.5 against Exceptions 1.0.5.0 while NuGet restored Exceptions 1.0.4",
    "GitHub Actions: Actions → CI - Pipeline. Branch model and feeds are in docs/ci.md. nuget.org Trusted Publishing setup is in docs/publishing.md (workflow publish-nuget.yml, Environment empty, secret NUGET_USER)",
    "NUGET_OUTPUT_DIR defaults to ~/nuget-local; state file is $NUGET_OUTPUT_DIR/.build-state",
    "BUILD_CONFIG defaults to Release",
    "build_nuget.py renders tooling READMEs once, then packs with SkipToolingDocsOnPack so each nupkg does not re-run tooling-docs",
    "--compile-only and --pack-only are mutually exclusive. CI pack runs compile then pack --no-build on the same runner",
    "ci_push.py skips a nupkg that already exists (409 Conflict) but fails the job if every package was a duplicate or any push hit a non-duplicate error",
    "After a successful nuget.org release on main, pipeline.yml runs ci_tag.py to push v{version} so the next auto-main run patch-bumps from that tag",
    "pipeline.yml writes a Markdown report via ci_summary.py to the Actions Summary tab (pack set, publish outcomes, skip reasons), rendered through scripts/docs/markdown.py",
    "unlist_nuget.py delists matching versions on nuget.org (not a hard delete). Needs NUGET_API_KEY with Unlist; Trusted Publishing OIDC cannot unlist"
  ],
  "readmePath": "scripts/nuget/README.md"
}
